# Required
DATABASE_URL=postgres://user:password@host:5432/dbname

# Recommended in production
DATABASE_SSL=true

# Keep recent server snapshots
MAX_BACKUPS=1000

# Secret used by scheduled backup trigger endpoint
BACKUP_CRON_SECRET=change-this-to-a-long-random-secret
